---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  href: string;
  icon?: string;
  iconSize?: number;
  iconPosition?: "left" | "right";
}

const {
  href,
  class: className,
  icon,
  iconSize = 24,
  iconPosition = "left",
  ...rest
} = Astro.props;
---

<a
  href={href}
  class:list={[
    "link-underline text-base-content hover:text-secondary transition-all duration-300 cursor-pointer text-lg flex items-center",
    iconPosition === "left" && "gap-2",
    iconPosition === "right" && "flex-row-reverse gap-0",
  ]}
  {...rest}
>
  {
    icon && (
      <span
        class="material-symbols-outlined header-icon"
        style={`font-size: ${iconSize}px;`}
      >
        {icon}
      </span>
    )
  }
  <slot />
</a>

<style>
  .link-underline {
    position: relative;
  }
  .link-underline::after {
    content: "";
    position: absolute;
    left: 50%;
    bottom: -4px;
    width: 0;
    height: 2px;
    background-color: currentColor;
    transform: translateX(-50%);
    transition: width 0.3s ease;
  }
  .link-underline:hover::after {
    width: calc(100% + 8px);
  }
</style>
